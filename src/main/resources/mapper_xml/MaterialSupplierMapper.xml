<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsh.erp.datasource.mappers.MaterialSupplierMapper">
  <resultMap id="BaseResultMap" type="com.jsh.erp.datasource.entities.MaterialSupplier">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="material_id" jdbcType="BIGINT" property="materialId" />
    <result column="supplier_id" jdbcType="BIGINT" property="supplierId" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    <result column="tenant_id" jdbcType="BIGINT" property="tenantId"/>
  </resultMap>

  <select id="queryByMaterialId" resultMap="BaseResultMap">
    select *
    from jsh_material_supplier
    where material_id = #{materialId}
  </select>

  <select id="queryByMaterialIds" resultMap="BaseResultMap">
    select *
    from jsh_material_supplier
    where material_id in
    <foreach collection="materialIds" item="item" separator="," open="(" close=")">
      #{item}
    </foreach>
  </select>

  <delete id="deleteByMaterialId">
    delete
    *   from jsh_material_supplier
    where material_id=
    #{materialId}
  </delete>

  <insert id="batchInsert" parameterType="com.jsh.erp.datasource.entities.MaterialSupplier">
    insert into jsh_material_supplier
    (id,supplier_id,create_time,material_id)
    values
    <foreach collection="models" separator="," item="item">
      (#{item.id},#{item.supplierId},#{item.createTime},#{item.materialId})
    </foreach>
  </insert>
</mapper>